apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-admin-view
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-admin-view
  template:
    metadata:
      labels:
        app: api-admin-view
    spec:
      containers:
        - name: api-admin-view
          image: quay.bancofiedesa.com.bo/openshift_laboratorio/api-admin-view:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: api-admin-view-service
spec:
  selector:
    app: api-admin-view
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: api-admin-view-route
spec:
  to:
    kind: Service
    name: api-admin-view-service
  port:
    targetPort: 3000
